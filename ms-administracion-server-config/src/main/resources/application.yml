server:
  port: 8888
  
spring:
  application:
    name: ms-admin-config-server

#Configuracion Local
  profiles:
    active: native
  cloud:
    config:
      import-check:
        enabled: false   # 🔥 SOLO desactiva validación
    server: 
        native:
          search-locations: classpath:/config  