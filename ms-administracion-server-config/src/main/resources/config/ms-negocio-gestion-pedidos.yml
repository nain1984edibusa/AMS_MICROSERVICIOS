server:
   port: 8093
spring:
  datasource: 
    url: jdbc:sqlserver://localhost;trustServerCertificate=true;databaseName=db_ms_pedidos
    username: sa
    password: P@ssword1234
    driver-class-name: com.microsoft.sqlserver.jdbc.SQLServerDriver
  jpa: 
    database-platform: org.hibernate.dialect.SQLServer2012Dialect
    hibernate: 
      ddl-auto: none
    show-sql: true  
    
# Server Registry Client
eureka:
   client:
      register-with-eureka: true
      fetch-registry: true
      serviceUrl:
         defaultZone: http://localhost:8889/eureka/
   instance:
      instance-id: ${spring.application.name}:${server.port}-${spring.application.instance_id:${random.value}}  	
      prefer-ip-address: true

#spring:
#  datasource: 
#    url: jdbc:mysql://mysql_productos_dev:3306/db_ms_productos?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true
#    username: userdev
#    password: Test@123
#    driver-class-name: com.mysql.cj.jdbc.Driver
#    hikari:
#      initializationFailTimeout: 0
#  jpa: 
#    database-platform: org.hibernate.dialect.MySQL8Dialect
#    hibernate: 
#      ddl-auto: update
#    show-sql: true
#
## Server Registry Client
#eureka:
#   client:
#      register-with-eureka: true
#      fetch-registry: true
#      serviceUrl:
#         defaultZone: http://eureka-server:8889/eureka
#   instance:
#      instance-id: ${spring.application.name}:${server.port}-${spring.application.instance_id:${random.value}}  	
#      prefer-ip-address: true
